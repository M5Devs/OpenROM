name: openrom_flutter
description: OpenROM — Universal ROM Compression Suite Desktop App
publish_to: 'none'
version: 2.2.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  desktop_drop: ^0.4.0
  file_picker: ^8.0.0
  window_manager: ^0.3.0
  shared_preferences: ^2.0.0
  path_provider: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - themes/
